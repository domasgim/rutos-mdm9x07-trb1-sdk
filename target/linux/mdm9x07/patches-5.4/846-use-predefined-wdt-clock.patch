--- a/drivers/watchdog/qcom-wdt.c
+++ b/drivers/watchdog/qcom-wdt.c
@@ -2,7 +2,6 @@
 /* Copyright (c) 2014, The Linux Foundation. All rights reserved.
  */
 #include <linux/bits.h>
-#include <linux/clk.h>
 #include <linux/delay.h>
 #include <linux/interrupt.h>
 #include <linux/io.h>
@@ -13,6 +12,8 @@
 #include <linux/watchdog.h>
 #include <linux/of_device.h>
 
+#define CLOCK_RATE_HZ 32765
+
 enum wdt_reg {
 	WDT_RST,
 	WDT_EN,
@@ -163,11 +164,6 @@ static const struct watchdog_info qcom_w
 	.identity	= KBUILD_MODNAME,
 };
 
-static void qcom_clk_disable_unprepare(void *data)
-{
-	clk_disable_unprepare(data);
-}
-
 static int qcom_wdt_probe(struct platform_device *pdev)
 {
 	struct device *dev = &pdev->dev;
@@ -177,7 +173,6 @@ static int qcom_wdt_probe(struct platfor
 	const u32 *regs;
 	u32 percpu_offset;
 	int irq, ret;
-	struct clk *clk;
 
 	regs = of_device_get_match_data(dev);
 	if (!regs) {
@@ -204,21 +199,6 @@ static int qcom_wdt_probe(struct platfor
 	if (IS_ERR(wdt->base))
 		return PTR_ERR(wdt->base);
 
-	clk = devm_clk_get(dev, NULL);
-	if (IS_ERR(clk)) {
-		dev_err(dev, "failed to get input clock\n");
-		return PTR_ERR(clk);
-	}
-
-	ret = clk_prepare_enable(clk);
-	if (ret) {
-		dev_err(dev, "failed to setup clock\n");
-		return ret;
-	}
-	ret = devm_add_action_or_reset(dev, qcom_clk_disable_unprepare, clk);
-	if (ret)
-		return ret;
-
 	/*
 	 * We use the clock rate to calculate the max timeout, so ensure it's
 	 * not zero to avoid a divide-by-zero exception.
@@ -227,7 +207,7 @@ static int qcom_wdt_probe(struct platfor
 	 * that it would bite before a second elapses it's usefulness is
 	 * limited.  Bail if this is the case.
 	 */
-	wdt->rate = clk_get_rate(clk);
+	wdt->rate = CLOCK_RATE_HZ;
 	if (wdt->rate == 0 ||
 	    wdt->rate > 0x10000000U) {
 		dev_err(dev, "invalid clock rate\n");
