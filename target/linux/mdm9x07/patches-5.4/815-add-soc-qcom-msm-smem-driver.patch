--- a/drivers/soc/qcom/Kconfig
+++ b/drivers/soc/qcom/Kconfig
@@ -194,6 +194,15 @@ config QCOM_APR
           used by audio driver to configure QDSP6
           ASM, ADM and AFE modules.
 
+config MSM_SMEM
+       depends on REMOTE_SPINLOCK_MSM
+       bool "MSM Shared Memory (SMEM)"
+       help
+         Support for the shared memory interface between the various
+         processors in the System on a Chip (SoC) which allows basic
+         inter-processor communication.
+
+
 config QCOM_RPM_SMD_LEGACY
 	bool "Qualcomm RPM driver using SMD protocol"
 	help
--- a/drivers/soc/qcom/Makefile
+++ b/drivers/soc/qcom/Makefile
@@ -25,3 +25,4 @@ obj-$(CONFIG_QCOM_LLCC) += llcc-qcom.o
 obj-$(CONFIG_QCOM_RPMHPD) += rpmhpd.o
 obj-$(CONFIG_QCOM_RPMPD) += rpmpd.o
 obj-$(CONFIG_QCOM_RPM_SMD_LEGACY) += rpm-smd.o
+obj-$(CONFIG_MSM_SMEM)  +=smem.o smem_state.o
