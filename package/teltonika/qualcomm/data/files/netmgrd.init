#!/bin/sh /etc/rc.common

START=09
STOP=80
USE_PROCD=1

start_service() {
	mkdir -p /var/run/netmgr
	chmod -R 777 /var/run/netmgr

	procd_open_instance
	procd_set_param command /usr/bin/netmgrd
	procd_close_instance
}
